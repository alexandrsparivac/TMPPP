# Runtime

Acest folder contine fisiere generate in timpul rularii locale.

- `data/` - fisiere locale de date sau exporturi.
- `logs/` - loguri generate de aplicatie sau de containerul Docker.

Continutul operational este ignorat de git prin `.gitignore`.
